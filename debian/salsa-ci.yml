include:
  - https://gitlab.com/hustle-ci/pipeline/raw/master/salsa-ci.yml
  - https://gitlab.com/hustle-ci/pipeline/raw/master/pipeline-jobs.yml

variables:
  RELEASE: 'groovy'
  SALSA_CI_DISABLE_AUTOPKGTEST: 'true'
  SALSA_CI_DISABLE_APTLY: 'false'
